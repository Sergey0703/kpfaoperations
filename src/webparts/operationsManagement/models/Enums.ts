// Enums for KPFA Operations Management

export enum DocumentType {
  Contract = 'Contract',
  Invoice = 'Invoice',
  Photo = 'Photo',
  Report = 'Report',
  Other = 'Other'
}

export enum DocumentStatus {
  Active = 'Active',
  Archived = 'Archived',
  Deleted = 'Deleted'
}

export enum ViewMode {
  Details = 'Details',
  Documents = 'Documents'
}
